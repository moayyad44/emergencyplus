# context.md — نظام عيادة emergencyplus

هذا الملف مرجع دائم للمشروع. أي محادثة قادمة (مع Claude أو أي مطوّر) يجب أن تبدأ بقراءته أولاً.

## 1. نظرة عامة
- **اسم العيادة:** emergencyplus
- **الغرض:** برنامج ويب (HTML) لإدارة عمليات الاستقبال اليومية في العيادة.
- **المستخدم الأساسي:** موظف الاستقبال (يُستخدم من أكثر من موظف، لذلك كل نصوص الواجهة والتعليمات موجّهة بصيغة المخاطب المذكر وليس المؤنث).
- **طريقة العمل:** ملف/تطبيق واحد يعمل من المتصفح. التخزين أصبح فعلياً على **Firebase Firestore** (سحابي، مشترك بين كل الأجهزة بشكل حي) بدل localStorage — انظر قسم "القرارات التقنية" للتفاصيل. أي إشارة لـ localStorage بباقي هذا الملف أصبحت تاريخية (كانت صحيحة قبل هذا الانتقال) والمرجع الفعلي الحالي هو Firestore.
- **اللغة:** عربي، اتجاه الصفحة RTL بالكامل.

## 2. الميزات المطلوبة (Scope)
| # | الميزة | الوصف | الحالة |
|---|--------|-------|--------|
| 1 | تسجيل الدخول | اسم مستخدم + كلمة مرور، يدور الآن بمصفوفة حسابات حقيقية متعددة (انظر البند 16) بدل حساب تجريبي واحد | ✅ تم تصميمه وبرمجته |
| 2 | الشاشة الرئيسية (Dashboard) | ملخص سريع + أزرار الوصول للميزات | ✅ تم تصميمه وبرمجته |
| 3 | تسجيل فاتورة جديدة | بحث برقم الهاتف → عرض/تسجيل المريض → اختيار عناصر → حفظ وطباعة | ✅ تم بناؤه بالكامل (نموذج أولي) |
| 4 | عرض فواتير مريض معيّن | تظهر تلقائياً ضمن شاشة الفاتورة الجديدة (وشاشة مريض الانتظار) عند اختيار مريض. كل فاتورة قابلة للضغط لعرضها كاملة للقراءة فقط مع إمكانية الطباعة | ✅ منجزة جزئياً (ضمن الشاشتين، ولا توجد شاشة مستقلة تجمع كل فواتير كل المرضى في مكان واحد — أقرب شيء لها حالياً هو شاشتا "الفواتير" (فواتير اليوم للمستخدم) و"تقييم المرضى" (كل الفواتير)، وكلتاهما بغرض مختلف) |
| 10 | حسابي | شاشة مستقلة من القائمة الجانبية تعرض معلومات المستخدم الحالي وتحتوي زر تسجيل الخروج (بعد إزالته من كل الشاشات الأخرى) | ✅ تم بناؤها بالكامل |
| 5 | ~~البحث عن مريض برقم التلفون (شاشة/بطاقة مستقلة)~~ | تم استبدال بطاقتها في الشاشة الرئيسية بميزة "الفواتير" (رقم 14) بناءً على طلب المستخدم. البحث الفعلي بالهاتف ما زال متاحاً كخطوة أولى داخل تدفق "فاتورة جديدة" | ❌ أُلغيت كبطاقة/شاشة مستقلة |
| 6 | ~~تسجيل مريض جديد (شاشة مستقلة)~~ | تم استبدال بطاقتها في الشاشة الرئيسية بميزة "مرضى بالانتظار" (رقم 9) بناءً على طلب المستخدم. تسجيل المريض الجديد ما زال متاحاً تلقائياً من داخل تدفق الفاتورة عند البحث برقم غير مسجّل | ❌ أُلغيت كشاشة مستقلة |
| 7 | ~~عرض كل الفواتير (بطاقة/شاشة مستقلة)~~ | تم استبدال بطاقتها في الشاشة الرئيسية بميزة "تقييم المرضى" (رقم 15) بناءً على طلب المستخدم | ❌ أُلغيت كبطاقة/شاشة مستقلة |
| 8 | تعديل أسعار العناصر + إضافة/حذف/ترتيب مواد وفئات | شاشة مستقلة من القائمة الجانبية لتغيير سعر كل عنصر، إضافة مادة أو فئة جديدة، حذف مادة، وإعادة ترتيب المواد/الفئات بالسحب والإفلات | ✅ تم بناؤها بالكامل |
| 9 | مرضى بالانتظار | قائمة انتظار يضيف لها الاستقبال المريض من بطاقة المريض بشاشة "فاتورة جديدة". المريض يبقى بالقائمة حتى يُحذف يدوياً (✕) أو تُنشأ له فاتورة فعلية. اختيار مريض من القائمة يفتح له شاشة بطاقة مستقلة ومميّزة بصرياً (وليست نفس شاشة "فاتورة جديدة"). الإضافة تمر عبر نافذة تأكيد مصممة (وليست نافذة المتصفح الافتراضية) يمكن فيها تعديل اسم المريض المحدد المرسل للطبيب | ✅ تم بناؤها بالكامل |
| 11 | فواتير غير مدفوعة | بطاقة الإحصائية بالشاشة الرئيسية قابلة للضغط، تفتح قائمة بكل الفواتير غير المدفوعة (اسم المريض، الهاتف، التاريخ، المبلغ). زر "تحديد كمدفوعة" يفتح نافذة اختيار طريقة الدفع (كاش/فيزا/كليك) قبل التحديد الفعلي، وإمكانية الضغط على الفاتورة لعرضها بنافذة القراءة فقط | ✅ تم بناؤها بالكامل |
| 12 | حالة المريض | بطاقة نصية بشاشة "عناصر الفاتورة" يكتب فيها الطبيب وصف حالة المريض/التشخيص، تُحفظ مع الفاتورة وتظهر في الطباعة وفي نافذة عرض الفاتورة السابقة إن وُجدت | ✅ تم بناؤها بالكامل |
| 13 | التحقق من رقم الهاتف | شاشة البحث برقم الهاتف ترفض أي رقم لا يتكوّن من 10 خانات ولا يبدأ بـ 07 | ✅ تم تفعيله |
| 14 | الفواتير (فواتير اليوم للمستخدم الحالي) | بطاقة بالشاشة الرئيسية (محل "بحث عن مريض" سابقاً) تفتح قائمة بكل الفواتير التي أنشأها المستخدم الحالي خلال اليوم فقط (بالاسم والمبلغ والحالة)، قابلة للضغط لعرض كل فاتورة بنافذة القراءة فقط | ✅ تم بناؤها بالكامل |
| 15 | تقييم المرضى | بطاقة بالشاشة الرئيسية (محل "كل الفواتير" سابقاً) تعرض كل الفواتير (اسم المريض، الهاتف، مبلغ الفاتورة) ليتصل الموظف بالمريض ويسجّل تقييمه من 1 إلى 5 نجوم مباشرة على الفاتورة | ✅ تم بناؤها بالكامل |
| 16 | نظام حسابات مستخدمين متعدد + صلاحيات ديناميكية | تسجيل الدخول يدور بمصفوفة حسابات حقيقية (`ep_users`). كل حساب له `username`/`password`/`fullName`/`role` (تشير لصلاحية من `ep_roles`). صلاحية "مدير" ثابتة بكل الشاشات دائماً، ولا يمكن حذفها أو تعديلها. المدير يقدر يضيف صلاحيات جديدة بأسماء مخصصة (مثال: محاسب) من شاشة "إدارة الصلاحيات" (رابط مستقل بالقائمة الجانبية، للمدير فقط)، ويحدد لكل صلاحية أي شاشات تشغيلية (فاتورة جديدة/الفواتير/مرضى بالانتظار/تقييم المرضى/تعديل الأسعار) مسموح لأصحابها برؤيتها — القائمة الجانبية تتحدث فوراً حسب صلاحية المستخدم المسجّل دخوله. شاشة "إدارة المستخدمين" (للمدير فقط) تسمح بإضافة حساب جديد (باختيار صلاحيته من القائمة الديناميكية) أو حذفه (مع منع حذف حسابك الحالي أو آخر حساب مدير). حذف صلاحية مرتبطة بحسابات موجودة ممنوع. حذف حساب موظف لا يمسح فواتيره القديمة. شاشة "حسابي" فيها نموذج لتغيير كلمة المرور الشخصية (يتطلب إدخال كلمة المرور الحالية) | ✅ تم بناؤها بالكامل |
| 17 | خصم على الفاتورة | من شاشة "تعديل الأسعار": بطاقة "إعدادات الخصم" يحدد فيها المدير نوع الخصم (قيمة ثابتة بالدينار **أو** نسبة مئوية — واحد منهم بس، مو الاثنين مع بعض) وأقصى قيمة/نسبة مسموح للموظف يعطيها للمريض. من شاشة الفاتورة الجديدة: خط أعداد (Slider) بأربع نقاط بس (٠٪ / ٢٥٪ / ٥٠٪ / ١٠٠٪ من أقصى خصم مسموح) يظهر تلقائياً فوق زر الحفظ إذا كان في خصم مفعّل (بيختفي كلياً لو المدير حط القيمة القصوى صفر). سحب الخط بيحدّث الإجمالي مباشرة، ويتصفّر تلقائياً مع كل فاتورة جديدة. الفاتورة المحفوظة، شاشة معاينة الفاتورة، والنسخة المطبوعة كلها بتعرض الإجمالي الفرعي وسطر الخصم بشكل منفصل عن الإجمالي النهائي إذا كان في خصم مطبّق | ✅ تم بناؤها بالكامل |
| 18 | كميات المواد بالفاتورة | كل مادة بشاشة اختيار عناصر الفاتورة صار عندها زري `+`/`−` بجانب صندوق التحديد (checkbox) — أول تحديد للمادة يبدأ بكمية 1، وبعدين تقدر تزيد/تنقص الكمية بحرية (النقص لكمية 1 وبعدها زر `−` مرة ثانية بيلغي تحديد المادة بالكامل). السعر الظاهر جنب كل مادة هو سعر السطر الكامل (سعر الوحدة × الكمية)، ويتحدّث فوراً مع كل ضغطة. الإجمالي والخصم كلهم يحسبون على أساس (سعر الوحدة × الكمية) لكل مادة. الفاتورة المحفوظة، ونافذة المعاينة، والنسخة المطبوعة، كلهم يعرضون عمود "الكمية" الجديد بجانب كل مادة | ✅ تم بناؤها بالكامل |
| 19 | مادة واحدة بأكثر من فئة | من شاشة "تعديل الأسعار": سحب مقبض مادة (⠿) وإفلاته على فئة مختلفة عن فئتها الأصلية **يضيفها لتلك الفئة أيضاً** (بنفس السعر المشترك، لأن السعر مربوط باسم المادة نفسه لا بالفئة) بدل رسالة المنع القديمة. المادة تبقى بفئتها الأصلية كمان — لإزالتها من فئة معيّنة فقط تُستخدم زر "✕" (حذف) الموجود بجانبها بتلك الفئة تحديداً، ولا يُحذف سعرها المشترك من `ep_item_prices` إلا لو صارت غير موجودة بأي فئة إطلاقاً | ✅ تم بناؤها بالكامل |
| 20 | تحديد فئة كاملة بالفاتورة | من شاشة "تعديل الأسعار": صندوق اختيار "تحديد الفئة كاملة بالفاتورة" بجانب اسم كل فئة، يفعّله المدير لكل فئة على حدة (معطّل افتراضياً لكل الفئات الموجودة والجديدة). من شاشة الفاتورة الجديدة: أي فئة مفعّلة هيك يظهر بجانب اسمها صندوق "تحديد الكل" — تحديده يختار كل مواد الفئة دفعة واحدة (بكمية 1 لكل مادة لم تكن محددة أصلاً)، وإلغاؤه يزيل كل مواد الفئة من الفاتورة. الصندوق يتحدّث تلقائياً (يتشيّك/يترك) حسب ما إذا كانت كل مواد الفئة محددة فردياً أم لا | ✅ تم بناؤها بالكامل |

## 3. نموذج البيانات المخطط له (Data Model)
لم يُنفَّذ بعد بالكامل، لكن هذا هو التصميم المتوقع لإرشاد البناء القادم:

**Patient (مريض)**
- id
- full_name (الاسم الكامل)
- phone (رقم التلفون — يُستخدم كمفتاح بحث أساسي)
- age / birth_date (اختياري)
- notes (ملاحظات طبية عامة، اختياري)
- created_at

**Invoice (فاتورة)** — تم تنفيذها فعلياً بهذا الشكل في `ep_invoices`:
- id (مثال: `EP-00001`، رقم تسلسلي عبر مستند `ep_store/invoice_seq` بـ Firestore)
- patient_id (ربط بسجل المريض الأساسي المرتبط برقم الهاتف)
- patient_name (اسم المريض الفعلي لهذه الفاتورة تحديداً — **مهم:** قد يختلف عن `full_name` في سجل المريض، لأن أكثر من مريض قد يشترك بنفس رقم الهاتف، مثل الأم وطفلها. يُعبَّأ تلقائياً باسم المريض الأساسي عند فتح شاشة العناصر لكنه قابل للتعديل قبل الحفظ)
- date (ISO string)
- items[] → كل عنصر `{ name, price, quantity }` (`price` هو سعر الوحدة كما هو مضبوط بشاشة الأسعار وقت الحفظ، و`quantity` هو العدد الذي اختاره الموظف عبر زري `+`/`−`؛ سعر سطر المادة الكامل = `price × quantity`. الفواتير القديمة المحفوظة قبل هذه الميزة لا تحتوي `quantity`، وكل الشاشات تتعامل معها بأمان باعتبار الكمية = 1)
- subtotal (مجموع كل أسعار الأسطر `price × quantity` لكل العناصر، قبل أي خصم)
- discount_type (`'value'` أو `'percentage'` أو `null` إن لم يُطبَّق خصم على هذه الفاتورة تحديداً)
- discount_percent_applied (النسبة الفعلية المطبَّقة كرقم، فقط إن كان `discount_type === 'percentage'` — تختلف عن أقصى نسبة مسموحة بشاشة الأسعار لأنها = أقصى نسبة × كسر نقطة خط الأعداد المختارة وقت الحفظ)
- discount_amount (قيمة الخصم الفعلية بالدينار، محسوبة وقت حفظ الفاتورة — 0 إن لم يُطبَّق خصم)
- total (= subtotal − discount_amount، لا يقل عن صفر)
- condition (نص حر يكتبه الطبيب لوصف حالة المريض/التشخيص عند إنشاء الفاتورة، اختياري — يظهر بالطباعة وبنافذة عرض الفاتورة السابقة إن لم يكن فارغاً)
- paid (true/false — افتراضياً false عند الإنشاء. **أصبح الآن قابلاً للتغيير فعلياً**: من شاشة "فواتير غير مدفوعة" — الوصول إليها بالضغط على بطاقة الإحصائية بالشاشة الرئيسية — بزر "تحديد كمدفوعة" لكل فاتورة)
- payment_method (نص: "كاش" / "فيزا" / "كليك" — يُسجَّل فقط عند تحديد الفاتورة كمدفوعة عبر نافذة اختيار طريقة الدفع. غير موجود على الفواتير غير المدفوعة بعد)
- rating (رقم من 1 إلى 5، أو غير موجود إن لم تُقيَّم بعد — يُسجَّل من شاشة "تقييم المرضى" بالضغط على عدد النجوم المناسب، ويمكن تعديله لاحقاً بإعادة الضغط على تقييم مختلف)
- created_by (اسم الموظف الحالي)

**Discount Settings (إعدادات الخصم)** — مستند واحد `ep_store/discount_settings`:
- type (`'value'` = قيمة ثابتة بالدينار، أو `'percentage'` = نسبة مئوية — واحد منهم بس بأي وقت، لا يوجد دعم لخصم قيمة + نسبة معاً)
- maxValue (أقصى قيمة/نسبة يقدر الموظف يطبّقها على أي فاتورة — القيمة الافتراضية عند الزرع الأول 0، أي الخصم معطّل كلياً حتى يضبطها المدير)
- خط الأعداد بشاشة الفاتورة له 4 نقاط ثابتة فقط دائماً: 0% / 25% / 50% / 100% من `maxValue` (وليس تدرّجاً حراً) — القيمة الفعلية المطبقة = `maxValue × كسر النقطة المختارة`

**Waiting List Entry (مريض بالانتظار)** — تُحفظ في `ep_waiting_list`:
- id (مثال: `w_<timestamp>`)
- patient_id (ربط بسجل المريض)
- patient_name (**الاسم المؤكَّد وقت الإضافة للانتظار — وليس بالضرورة نفس `full_name` في سجل المريض.** لأن أكثر من مريض قد يشترك بنفس رقم الهاتف (الأم وطفلها مثلاً)، تظهر نافذة تأكيد مصممة عند الضغط على "أضف مريض بالانتظار" تسمح للموظف بكتابة/تعديل اسم المريض الفعلي الذاهب للطبيب قبل الحفظ. هذا الاسم هو ما يظهر في شاشة "مرضى بالانتظار" وفي عنوان بطاقة المريض عند فتحها من هناك، ويُستخدم أيضاً كتعبئة مبدئية لاسم المريض عند إنشاء الفاتورة)
- phone
- added_at (ISO string)
- تُضاف من زر "أضف مريض بالانتظار" في بطاقة المريض (خطوة 2 من تدفق الفاتورة الجديدة) عبر نافذة تأكيد مخصصة (`confirmWaitingModal`) — **وليس نافذة `confirm()` الافتراضية من المتصفح.**
- **قاعدة الحذف (مهمة):** المريض لا يُحذف من قائمة الانتظار إلا في حالتين فقط:
  1. الضغط اليدوي على زر الحذف (✕) بجانب اسمه في شاشة "مرضى بالانتظار".
  2. إنشاء فاتورة فعلية له بنجاح (بغض النظر عن المسار — سواء دخل لبطاقته من شاشة الانتظار أو من بحث جديد بنفس رقم الهاتف).
- الضغط على اسم مريض من شاشة "مرضى بالانتظار" **لا يحذفه** من القائمة — فقط ينقل لبطاقته (الفواتير السابقة + زر إدخال فاتورة). يبقى بالقائمة حتى لو رجع الموظف بدون إنشاء فاتورة له (مثلاً لو غيّرت رأيها أو احتاجت البحث عن مريض آخر أولاً).

**كتالوج الأدوية والمستلزمات (Item Catalog)**
- مأخوذ من نموذج ورقي داخلي للعيادة (صورة مرفقة من المستخدم) يحتوي 5 أقسام:
  1. مستلزمات عامة (Vital Signs، قفازات، محاليل وريدية...)
  2. حقن وقساطر وجبائر (Syringes، Nebulizer، Foleys، Casts...)
  3. ضمادات وخيوط جراحية (Gauze، Proline، Vicryl...)
  4. أدوية (Votrex، Dexamed، Ventolin...)
  5. أدوية وفحوصات ورسوم (Lincomycin، Lab Test، E.C.G، DR-Fees...)
- كل عنصر حالياً بسعر ثابت **10 دينار افتراضياً** (`DEFAULT_ITEM_PRICE` في الكود)، لكن يمكن تخصيص سعر مستقل لكل عنصر من شاشة "تعديل الأسعار" (تُحفظ في مستند `ep_store/item_prices` بـ Firestore، بصيغة `{ "اسم العنصر": السعر }`). أي عنصر غير موجود بهذا المفتاح يُستخدم له السعر الافتراضي. **السعر مربوط باسم المادة نفسه، وليس بالفئة** — فلو ظهرت نفس المادة بأكثر من فئة (انظر البند 19 بجدول الميزات)، تشترك بنفس السعر أينما ظهرت.
- الكتالوج نفسه أصبح **قابلاً للتعديل**: يبدأ من قائمة افتراضية ثابتة في الكود (`DEFAULT_ITEM_CATALOG`، مأخوذة من الصورة المرفقة)، لكنه يُنسخ عند أول تشغيل إلى مستند `ep_store/item_catalog` بـ Firestore، وبعدها كل إضافة/حذف مادة تُطبَّق على هذه النسخة المحفوظة فقط (لا تُعدَّل القائمة الافتراضية بالكود).
- **إضافة مادة جديدة:** من شاشة "تعديل الأسعار" — اسم + فئة (تُختار من قائمة منسدلة تشمل كل الفئات الموجودة، بما فيها أي فئة أُضيفت يدوياً) + سعر اختياري (يُستخدم السعر الافتراضي إن تُرك فارغاً).
- **إضافة فئة جديدة:** نموذج منفصل بجانب نموذج إضافة المادة — يُدخل اسم الفئة فقط، وتُضاف فارغة (بدون مواد) إلى الكتالوج، وتصبح فوراً متاحة كخيار عند إضافة مادة جديدة.
- **حذف مادة:** زر حذف بجانب كل مادة في شاشة "تعديل الأسعار"، مع تأكيد قبل الحذف. الحذف لا يؤثر على الفواتير السابقة المحفوظة (لأن كل فاتورة تحتفظ بنسخة من اسم وسعر العنصر وقت إنشائها). الحذف يزيل المادة من الفئة المحدَّدة فقط — لو كانت موجودة بفئات أخرى كمان (البند 19)، تبقى موجودة فيها وسعرها المشترك لا يُحذف من `ep_item_prices` إلا لو لم تعد موجودة بأي فئة إطلاقاً. **لا توجد حالياً إمكانية حذف فئة كاملة** (فقط إضافتها وإعادة ترتيبها).
- **إعادة الترتيب بالسحب والإفلات، ونقل/نسخ مادة لفئة أخرى:** كل فئة وكل مادة لها مقبض سحب (أيقونة ⠿). سحب فئة كاملة يغيّر ترتيب الفئات. سحب مادة وإفلاتها **داخل نفس الفئة** يعيد ترتيبها بين مواد الفئة. سحب مادة وإفلاتها على **فئة مختلفة** (سواء فوق مادة موجودة فيها أو بمساحة فارغة بالفئة) **يضيفها لتلك الفئة أيضاً** (نفس الاسم والسعر المشترك) دون حذفها من فئتها الأصلية — هذا ما يسمح لمادة واحدة بالظهور تحت أكثر من فئة (البند 19 بجدول الميزات؛ كان هذا ممنوعاً برسالة تنبيه قبل 2026-09-12). الترتيب/الإضافة الجديدة تُحفظ فوراً بمستند `ep_store/item_catalog` وتنعكس مباشرة على شاشة الفاتورة الجديدة. **ملاحظة:** السحب والإفلات هنا مبني بتقنية المتصفح القياسية (HTML5 Drag & Drop) التي تعمل بشكل موثوق بالماوس على حاسوب/لابتوب، لكنها غير مدعومة جيداً باللمس على الجوال/التابلت — إن احتاجت العيادة استخدام هذه الشاشة من جهاز لمسي، سيلزم لاحقاً استبدالها بأزرار "تحريك لأعلى/لأسفل" أو مكتبة سحب متوافقة مع اللمس.
- **تحديد فئة كاملة بالفاتورة (البند 20):** من شاشة "تعديل الأسعار"، صندوق اختيار بجانب اسم كل فئة يفعّل/يعطّل ظهور خيار "تحديد الكل" لهذه الفئة تحديداً بشاشة الفاتورة الجديدة — مُعطّل افتراضياً لكل فئة، ويُحفظ التفعيل بمستند `ep_store/category_select_all` (كائن `{ "اسم الفئة": true }`، الفئات غير المذكورة تُعتبر معطّلة). عند تفعيله، الموظف يقدر يحدد/يلغي كل مواد الفئة دفعة واحدة بضغطة واحدة أثناء تعبئة الفاتورة.
- الموظف "ينقر" فقط على العناصر المستخدمة (checkbox) أثناء تعبئة الفاتورة، والسعر يظهر بجانب كل عنصر تلقائياً حسب آخر تحديث من شاشة الأسعار.

**User (مستخدم/موظف استقبال)** — تم تنفيذه فعلياً بهذا الشكل في `ep_users`:
- id (مثال: `u_1` للحساب الافتراضي المزروع، أو `u_<timestamp>` للحسابات المضافة لاحقاً)
- username (الحساب الافتراضي المزروع أصبح `Admin` بدل `reception` القديم — تمت هجرة الاسم تلقائياً لمرة واحدة لمن كان قد أنشأ الحساب مسبقاً)
- password (حالياً محفوظة كنص عادي في localStorage للنموذج الأولي فقط — **يجب استبدالها بتشفير/سيرفر حقيقي قبل الاستخدام الفعلي**؛ يمكن للمستخدم تغييرها بنفسه من شاشة "حسابي")
- fullName
- role (معرّف يشير إلى صلاحية من `ep_roles` — وليس نص ثابت "admin/staff" فقط)

**Role (صلاحية)** — مخزّنة في `ep_roles`:
- id (`admin` ثابت للصلاحية المدمجة، أو `role_<timestamp>` للصلاحيات المخصصة)
- name (اسم عرض الصلاحية، مثال: "مدير"، "موظف استقبال"، أو أي اسم يضيفه المدير مثل "محاسب")
- screens (مصفوفة معرّفات الشاشات التشغيلية المسموح لهذه الصلاحية برؤيتها من: `invoice`, `myinvoices`, `waiting`, `ratings`, `prices`)
- builtin (`true` فقط لصلاحية "مدير" — تمنعها من التعديل أو الحذف، ولها دائماً كل الشاشات بما فيها "إدارة المستخدمين" و"إدارة الصلاحيات" نفسها)

ملاحظة: شاشتا "الرئيسية" و"حسابي" متاحتان لكل المستخدمين بغض النظر عن الصلاحية، وشاشتا "إدارة المستخدمين"/"إدارة الصلاحيات" مقصورتان على صلاحية "مدير" المدمجة فقط ولا يمكن منحهما لصلاحية مخصصة (قرار تصميمي لتفادي أن يمنح المدير بالخطأ صلاحية إنشاء حسابات/صلاحيات أخرى لغير المدير).

## 4. القرارات التقنية
- ملف واحد `index.html` يحتوي HTML + CSS + JS (بدون build tools)، ليسهل فتحه وتعديله مباشرة.
- التخزين: **Firebase Firestore** (سحابي، حقيقي، مشترك بين كل الأجهزة بشكل حي) — تم الانتقال إليه من localStorage بتاريخ 2026-09-12. مشروع Firebase: `emergencyplus-ff4c4`.
  - **بنية البيانات (محدَّثة 2026-09-16 — انظر ملاحظة الفواتير أدناه):** مجموعة (Collection) اسمها `ep_store`، وبداخلها مستند (Document) واحد لكل "جدول" قديم كان بـ localStorage، وكل مستند فيه حقل واحد اسمه `value` يحمل نفس البيانات القديمة بالضبط (مصفوفة أو كائن): `users`, `roles`, `patients`, `waiting_list`, `item_prices`, `item_catalog`, `invoice_seq`, `discount_settings`, `category_select_all`, `quick_invoice_settings`. **"invoices" لم تعد ضمن هذه القائمة** — انتقلت لمجموعة مستقلة `ep_invoices` (انظر التفصيل الكامل بقسم "سجل التقدّم" تحديث 2026-09-16). هذا القرار الأصلي (مستند واحد بقيمة JSON كاملة بدل مستند منفصل لكل سجل، لبقية الجداول غير الفواتير) اختير عمداً لتقليل حجم إعادة الكتابة على الكود القديم — كل دوال `getXxx()/saveXxx()` بقيت بنفس التوقيع (متزامنة/synchronous) وبس تغيّر مصدرها الداخلي.
  - **آلية العمل:** كائن JS محلي اسمه `store` يُعبّأ من Firestore عبر مستمعين حيّين (`onSnapshot`) على كل مستند من الثمانية أعلاه. `getXxx()` يقرأ من `store` مباشرة (سريع، متزامن). `saveXxx()` يحدّث `store` فوراً (تحديث تفاؤلي/optimistic) ويبعت نسخة لـ Firestore بالخلفية عبر `writeStoreDoc()` بدون انتظار (`fire-and-forget`) — **معناها احتمال ضئيل لفقدان آخر تعديل لو أُغلق المتصفح فوراً قبل اكتمال الإرسال للشبكة**، مقبول لتطبيق استقبال عيادة صغيرة.
  - **التحميل الأول:** الصفحة تعرض شاشة "جاري تحميل البيانات..." (فوق نموذج تسجيل الدخول) لحين وصول أول نسخة من كل المستندات الثمانية (`dataReady` promise)، وبعدها فقط تُشغَّل عمليات "الزرع لأول مرة" (`runOneTimeSeeding()`: صلاحية مدير، حساب Admin الافتراضي، ترحيل اسم reception القديم، كتالوج الأصناف الافتراضي) واستعادة الجلسة المحفوظة وعرض نموذج الدخول.
  - **التحديث الحي بين الأجهزة:** بعد التحميل الأول، أي تعديل يصل من جهاز/مستخدم آخر عبر `onSnapshot` يُحدّث `store` وينادي `refreshCurrentViewFor(key)` التي تعيد رسم الشاشة المفتوحة حالياً فقط لو كانت معنية بالتغيير (مثال: تعديل قائمة الانتظار من جهاز آخر بيحدّث شاشة "مرضى بالانتظار" وبطاقة "مرضى بالانتظار" بالشاشة الرئيسية تلقائياً لأي مستخدم فاتحهم بنفس اللحظة، بدون تحديث الصفحة يدوياً).
  - **جلسة الدخول** (`ep_current_user`) بقيت في `sessionStorage` (محلية لكل جهاز/متصفح عمداً) — مش جزء من المزامنة السحابية، لأنه "مين مسجّل دخول على هالجهاز بالذات" مش بيانة مشتركة.
  - **صلاحيات قاعدة البيانات (Firestore Rules):** حالياً مفتوحة بالكامل (`allow read, write: if true`) لأنه التطبيق لا يستخدم Firebase Authentication بعد — أي حد يعرف بيانات `firebaseConfig` (نفسها موجودة بكود الصفحة، أي أنها مكشوفة لأي زائر للموقع) يقدر يوصل للبيانات مباشرة من خارج التطبيق. **هذا قرار مؤقت مقبول لتطبيق داخلي صغير، وليس آمناً بمعايير الإنتاج العامة** — يلزم لاحقاً إضافة Firebase Authentication وتضييق القواعد إن احتاجت العيادة مستوى حماية أعلى.
  - **قيود معروفة سابقاً، حُلّت 2026-09-16:** كان حجم المستند الواحد بـ Firestore (1 ميجابايت ثابتة من Google) يحدّ عدد الفواتير القابلة للتخزين إجمالاً بحوالي 1000–2100 فاتورة (لأنها كانت كلها بمستند واحد). **تم حلّ هذا نهائياً** بنقل الفواتير لمجموعة `ep_invoices` (مستند مستقل لكل فاتورة) — التفصيل الكامل بقسم "سجل التقدّم" تحديث 2026-09-16. بقية جداول `ep_store` (المرضى، المستخدمين، الكتالوج...) ما زالت بنفس نمط "مستند واحد" القديم وقد تحتاج نفس المعالجة يوماً ما إن كبر حجمها كثيراً، لكن هذا غير متوقع قريباً (مثلاً: آلاف المرضى فقط، لا آلاف الفواتير، وحجم كل سجل مريض أصغر بكثير من الفاتورة).
- الخط: IBM Plex Sans Arabic (عبر Google Fonts) — خط عربي عملي وواضح لبيئة عمل يومية.
- تسجيل الدخول متعدد المستخدمين مع صلاحيات ديناميكية قابلة للتخصيص (انظر البند 16 بجدول الميزات ونموذجي User وRole أعلاه). الحساب الافتراضي المزروع تلقائياً عند أول تشغيل هو `Admin` / `12345` (الاسم الكامل: سارة، صلاحية مدير). **كلمات المرور ما زالت مخزّنة كنص عادي (بدون تشفير) وهذا غير آمن — يلزم تشفير حقيقي أو Firebase Authentication قبل الاستخدام الفعلي بمعايير إنتاج حقيقية.**
- **بطاقة المريض أصبحت شاشتين منفصلتين في HTML** (`invoiceStepPatient` داخل تدفق "فاتورة جديدة"، و`view-waiting-patient` مستقلة لتدفق "مرضى بالانتظار")، تتشاركان فقط شاشة "عناصر الفاتورة" (`invoiceStepItems`). متغيّر JS باسم `invoiceEntrySource` (قيمته `'phone'` أو `'waiting'`) يحدد أي شاشة تظهر عند الرجوع ("تغيير المريض" أو "رجوع لبطاقة المريض")، وأي شاشة يُرجع إليها المستخدم بعد حفظ الفاتورة بنجاح.
- **عرض فاتورة سابقة للقراءة فقط:** نافذة منبثقة (`viewInvoiceModal`) تُفتح بالضغط على أي صف من "الفواتير السابقة" في أي من الشاشتين، تعرض العناصر والإجمالي كجدول ثابت (بدون حقول تعديل)، مع زر طباعة منفصل يستخدم نفس منطقة الطباعة المخفية المستخدمة عند إنشاء فاتورة جديدة.

### النشر والاستضافة (Deployment) — بدأ 2026-09-12، انتقل إلى GitHub Pages لاحقاً
- **المستودع:** الكود مرفوع على GitHub، مستودع `moayyad44/emergencyplus` (حساب المستخدم الشخصي)، يحتوي `index.html` و`context-3.md` بجذر المستودع مباشرة.
- **الاستضافة الفعلية (محدّثة 2026-09-16): GitHub Pages** — تم التخلي عن خطة Netlify الأصلية (المذكورة سابقاً بهذا القسم) والانتقال مباشرة لـ GitHub Pages، مبني من فرع `main` بجذر المستودع (`root`)، نشر تلقائي (Continuous Deployment) عند أي `push` على `main`.
- **الدومين المخصص:** `eplus.life` (مسجّل على GoDaddy)، مربوط كـ Custom domain بإعدادات GitHub Pages، وملف `CNAME` موجود بجذر المستودع يحمل نفس القيمة. سجلات DNS بـ GoDaddy مضبوطة صح (4 سجلات A للـ apex تشاور على عناوين GitHub Pages القياسية 185.199.108/109/110/111.153، وCNAME لـ `www` يشاور على `moayyad44.github.io`).
- **HTTPS:** ✅ مفعّل بالكامل (Enforce HTTPS) منذ 2026-09-16 — كانت معلّقة لفترة بسبب "DNS Check in Progress" من طرف GitHub (وليس خللاً بإعدادات الـ DNS نفسها)، وبعد اكتمال الفحص صدرت الشهادة تلقائياً وفعّلها المستخدم يدوياً من `github.com/moayyad44/emergencyplus/settings/pages`.
- **طريقة التحديث:** أي تعديل على الكود يُرفع مباشرة كـ commit جديد على GitHub (عادة عبر GitHub API من بيئة عمل Claude، انظر الملاحظة أدناه)، وينعكس تلقائياً على الموقع المباشر خلال دقيقة أو أقل — لا حاجة لأي خطوة يدوية إضافية.
- **ملاحظة (منذ 2026-09-12، ما زالت سارية):** المستخدم يعطي Claude توكن GitHub (Fine-grained Personal Access Token) بصلاحية Read/write على مستودع `emergencyplus` تحديداً بدايةَ كل محادثة يريد فيها تعديل الكود، فيقدر Claude يرفع تعديلات مباشرة عبر GitHub API (`curl`، لأن `api.github.com` ضمن النطاقات المسموحة بشبكة الحاوية — لا تُخزَّن هذه التوكنات بالذاكرة طويلة الأمد لأسباب أمنية، تُعطى بكل محادثة من جديد). هذا التوكن مؤقت وقد يُلغى/يُجدَّد من طرف المستخدم بأي وقت من إعدادات GitHub. **Firebase Firestore Rules ولوحة تحكم GoDaddy/DNS تبقيان دائماً يدويتين** — لا توجد طريقة لـ Claude للوصول لهما برمجياً من بيئة العمل الحالية (لا واجهة API متاحة أو النطاق غير مسموح بشبكة الحاوية).

## 5. هوية التصميم (Design Tokens)
- **الألوان:**
  - خلفية عامة: `#F3F6F5`
  - سطح البطاقات: `#FFFFFF`
  - أساسي (أخضر مزرق داكن): `#0E4749`
  - أساسي غامق: `#0A3436`
  - إشارة الطوارئ/التمييز (أحمر): `#D6473E`
  - نص ثانوي: `#5E7876`
  - خطوط فاصلة: `#DDE4E2`
  - **لون سياقي لقائمة الانتظار (كهرماني):** `#B36A21` — يُستخدم حصراً لتمييز أي شاشة/عنصر متعلق بـ"مرضى الانتظار" (الشارة، الشريط الجانبي لبطاقة المريض القادم من الانتظار) حتى تُميَّز بصرياً فوراً عن شاشة "فاتورة جديدة" العادية (التي تستخدم اللون الأساسي).
- **الشعار:** علامة "+" (بلس) مبنية بـ SVG، مستوحاة من اسم "emergencyplus" نفسه، تُستخدم كشعار وكنمط خلفية خفيف بدل أي أيقونة طبية جاهزة.
- **الأسلوب:** بطاقات مسطّحة بدون ظلال ناعمة متكررة، فاصل بسيط بلون واحد لتمييز الحالة النشطة، بدون تدرجات لونية زخرفية.

## 6. سجل التقدّم (Log)
- **2026-09-17:** تأكيد المستخدم يدوياً أن قواعد Firestore Security Rules صارت مفتوحة (`allow read, write: if true`) بصيغة تشمل كل المجموعات — ظهر تحذير Firebase القياسي ("Your security rules are defined as public...") وهو متوقع/طبيعي بهذا الإعداد (حالة قائمة من قبل هذا التاريخ لبقية بيانات التطبيق أصلاً، وليست خللاً جديداً سببه ترحيل الفواتير). بهذا، مجموعة `ep_invoices` الجديدة (انظر تحديث 2026-09-16 السادس أعلاه) مغطّاة بنفس القواعد ومؤكَّد أنها تعمل.
- **إصلاح مظهر بطاقة "الخصم" بشاشة الفاتورة (طلب المستخدم — "شريط الخصم منظره مش حلو"):** السبب الجذري كان متغيّر CSS غير معرَّف `var(--border)` (لا وجود له بـ`:root`) مستخدَم بـ`.discount-card` فجعل حدّها يختفي بصمت (إعلان `border` غير صالح كاملاً بدون قيمة احتياطية صالحة). تم:
  - استبدال `var(--border)` بـ `var(--line)` (المتغيّر الفعلي المستخدم لحدود الشاشة بباقي الكود) — ووُجد نفس الخطأ بـ3 أماكن أخرى غير مرتبطة بالخصم (سبينر التحميل `.data-loading .spinner`، بطاقات الأدوار `.role-card` بشاشة "إدارة الصلاحيات"، وأزرار +/- بكمية العنصر `.qty-btn` بشاشة الفاتورة) — أُصلحت كلها بنفس التعديل (`sed` عام على الملف كامل لكل `var(--border)`).
  - إعادة تصميم شريط `#discountSlider` نفسه: مقبض (thumb) وممر (track) مخصّصان بلون العلامة التجارية (`--primary`) بدل مظهر المتصفح الافتراضي (عبر `::-webkit-slider-thumb` و`::-moz-range-thumb/track`)، وشارة قيمة الخصم (`#discountValueLabel`) صارت "pill" بخلفية فاتحة بدل نص أحمر عادي.
  - **إصلاح منطقي إضافي بالمقياس تحت الشريط (`#discountScale`):** كان يعرض دائماً "٠٪ ٢٥٪ ٥٠٪ ١٠٠٪" ثابتة بالـHTML بغض النظر عن نوع الخصم الفعلي المُعدَّ من شاشة "تعديل الأسعار" — مضلِّل تماماً لو نوع الخصم "قيمة ثابتة" (د.أ) وليس نسبة. الآن يُبنى ديناميكياً بجافاسكربت داخل `renderDiscountUI()` حسب `settings.type`: نسب مئوية من الحد الأقصى المُعدّ إن كان النوع "نسبة"، أو مبالغ دينار فعلية إن كان النوع "قيمة ثابتة".

- **2026-09-16 (تحديث سادس):** ترحيل تخزين الفواتير من مستند Firestore واحد (`ep_store/invoices`، مصفوفة تحوي كل الفواتير) إلى مجموعة مستقلة `ep_invoices` بمستند خاص لكل فاتورة (معرّف المستند = رقم الفاتورة نفسه، مثل `EP-00001`) — **يلغي نهائياً** سقف الـ~1000–2100 فاتورة الذي كان مفروضاً بحد حجم المستند الواحد (1 ميجابايت من Google). تم التنفيذ فيما لا توجد أي فاتورة حقيقية مسجّلة بالإنتاج بعد، فلم يلزم أي ترحيل بيانات (data migration) — تغيير بنيوي بحت بالكود:
  - `STORE_KEYS` (القائمة العامة لمفاتيح `ep_store` المتزامنة عبر `onSnapshot`) لم تعد تحوي `'invoices'`. بدلها، مستمع `onSnapshot` منفصل على `db.collection('ep_invoices')` يملأ `store.invoices` من كل مستندات المجموعة (`snap.docs.map(d => d.data())`) في كل مرة، ويشارك بنفس آلية `_loadedKeys`/`dataReadyResolve` (عبر ثابت جديد `TOTAL_STORE_LISTENERS = STORE_KEYS.length + 1`) ونفس استدعاء `refreshCurrentViewFor('invoices')` للتحديثات الحية بعد أول تحميل — سلوك متطابق تماماً لبقية الشاشة من منظور المستخدم.
  - `getInvoices()` بقيت بنفس الشكل تماماً (`return store.invoices`) فكل نقاط القراءة بالكود (فلترة، بحث، ترتيب...) لم تحتج أي تعديل.
  - الدالة `saveInvoices(list)` (كتابة استبدالية لكل المصفوفة دفعة وحدة) حُذفت نهائياً، ومكانها دالة جديدة `saveInvoice(invoice)` تكتب/تحدّث **فاتورة واحدة فقط** (`db.collection('ep_invoices').doc(invoice.id).set(invoice)`) مع تحديث تفاؤلي فوري لعنصرها المطابق داخل `store.invoices` محلياً. عُدِّلت الأربع نقاط الوحيدة اللي كانت تكتب فواتير لتستخدمها: إنشاء/تعديل فاتورة عادية (`saveInvoiceBtn`)، إنشاء/تعديل فاتورة سريعة IM (`quickInvoiceForm`)، "تحديد كمدفوعة" (`payment-method-btn`)، وتقييم المريض بالنجوم (`.star` click).
  - `nextInvoiceId()` (يعتمد على `ep_store/invoice_seq` منفصل) لم يتأثر إطلاقاً — الترقيم التسلسلي للفواتير مستمر بلا تغيير.
  - **⚠️ نقطة تحتاج تأكيد المستخدم يدوياً:** قواعد أمان Firestore (Security Rules) الحالية مفتوحة (`allow read, write: if true`) — لازم التأكد إنها مكتوبة بصيغة تشمل *كل* المجموعات (مثل `match /{document=**}` بجذر قاعدة البيانات) وليس مقصورة صراحة على `ep_store` فقط، وإلا فشلت كل عمليات القراءة/الكتابة على مجموعة `ep_invoices` الجديدة بخطأ صلاحيات (permission-denied). Claude لا يملك أي وصول برمجي لفحص أو تعديل Firestore Rules من بيئة العمل الحالية — يلزم تحقق يدوي من Firebase Console (Firestore Database → Rules)، ويُفضَّل اختبار إنشاء فاتورة حقيقية فوراً بعد هذا التحديث للتأكد من نجاح الحفظ.
  - **ملاحظة:** مستند `ep_store/invoices` القديم (كان فاضياً أصلاً، لم يُستخدم مطلقاً بالإنتاج) بقي موجوداً كأثر ميت غير مستخدَم بقاعدة البيانات — غير ضار، يمكن حذفه يدوياً من Firebase Console لاحقاً إن رغب المستخدم، وليس ضرورياً.

- **2026-09-16 (تحديث خامس):** إضافة اسم الموظف صاحب الحساب (`invoice.created_by`) على كل صفحات طباعة الفاتورة — سطر "الموظف: <الاسم>" أُضيف لصف `pi-meta` بالصفحة الرئيسية (`printCreatedBy`)، وبنفس السطر المختصر لصفحات روشتة/التحاليل المخبرية/التقرير الطبي (`printPrescriptionCreatedBy` / `printLabCreatedBy` / `printReportCreatedBy`). دالة `populatePrintArea` صار عندها باراميتر جديد `createdBy`، ومررناه بكل نقاط الاستدعاء الثلاث (الحفظ الأول لفاتورة عادية، الحفظ الأول لفاتورة سريعة IM، وإعادة الطباعة من نافذة معاينة فاتورة سابقة) من حقل `invoice.created_by` (أو `inv.created_by` بحالة الإعادة) — يعني القيمة المطبوعة هي **مين أنشأ الفاتورة أصلاً**، مش بالضرورة مين ضاغط زر الطباعة الآن (لو المستخدمين مختلفين).

- **2026-09-16 (تحديث رابع):** حل مشكلة HTTPS على eplus.life التي كانت معلّقة من بداية هجرة الاستضافة (انظر قسم "النشر والاستضافة" بالأعلى) — تبيّن إنها كانت مجرد "DNS Check in Progress" من طرف GitHub (لم تكن مشكلة بإعدادات الـ DNS نفسها، وتأكدنا حينها إنها مضبوطة صح عبر فحص A/CNAME records مباشرة)؛ بعد ما اكتمل الفحص من طرف GitHub صدرت الشهادة تلقائياً وصار "Enforce HTTPS" متاحاً، وفعّله المستخدم من صفحة إعدادات الريبو (`github.com/moayyad44/emergencyplus/settings/pages`). الموقع يعمل الآن بـ HTTPS كامل.

- **2026-09-16 (تحديث ثالث):** إعادة ترتيب شاشة "تعديل الأسعار" لتقليل الازدحام البصري — طلب تحسين واجهة (ليس من البنود التسعة الأصلية، اكتملت جميعها بالتحديث السابق)، جاء بعدما صارت الشاشة معجّقة نتيجة تراكم الميزات (خصوصاً فئة "التحاليل المخبرية" بـ150 مادة):
  - الأربع بطاقات إعدادات (إضافة مادة، إضافة فئة، إعدادات الخصم، إعدادات الفاتورة السريعة IM) صارت مطوية افتراضياً داخل عنصر `<details>` واحد (`prices-settings-accordion`) بعنوان "الإعدادات والإضافات" — بدون أي جافاسكربت، العنصر الأصلي بالمتصفح كافٍ للطي/الفتح.
  - قائمة الكتالوج (`#pricesCatalog`) تحوّلت من شبكة 3 أعمدة (`.items-catalog{grid-template-columns:repeat(3,1fr)}`) إلى عمود واحد فقط (`grid-template-columns:1fr`) من بطاقات فئات قابلة للطي/التوسيع فردياً (زر سهم `.category-toggle-btn` بجانب اسم كل فئة، تُطوى موادها `.category-items` بـ`display:none` حتى تُفتح) — **مطوية افتراضياً كلها** عند فتح الشاشة، مع عدّاد عدد المواد بجانب اسم كل فئة.
  - حالة الطي/التوسيع محفوظة بمتغير جافاسكربت `pricesExpandedCategories` (Set بأسماء الفئات المفتوحة) تبقى محفوظة عبر إعادة الرسم داخل نفس الجلسة (لا تُصفَّر إلا عند مغادرة الشاشة والعودة إليها من القائمة الجانبية).
  - مربع بحث جديد (`#pricesSearchInput`) أعلى القائمة يبحث باسم الفئة أو المادة؛ الفئة المطابق اسمها تظهر كاملة، وأي فئة أخرى تظهر مواد فُلترت حسب التطابق فقط، وتُفتح تلقائياً أي فئة فيها تطابق أثناء البحث (بغض النظر عن حالة الطي المحفوظة).
  - زرّا "توسيع الكل" / "طي الكل" (`#expandAllCategoriesBtn` / `#collapseAllCategoriesBtn`) فوق القائمة لتغيير حالة كل الفئات دفعة واحدة.
  - لا تغيير على منطق تعديل الأسعار، إعادة ترتيب السحب والإفلات، أو حذف/تسمية الفئات والمواد نفسه — التعديل بصري/تنظيمي بالكامل.

- **2026-09-16 (تحديث ثانٍ):** الطباعة المنفصلة لكل قسم بصفحة مستقلة — **آخر بند من كل البنود التسعة المتفق عليها بترتيب المهمات الأصلي، وبهذا اكتملت جميعها.** إعادة هيكلة منطقة الطباعة المخفية `#printInvoice` من حاوية واحدة متصلة إلى 4 صفحات مستقلة (`.print-page`، كل واحدة بحشوتها الخاصة `padding` بدل حشوة الحاوية الواحدة سابقاً): `printPageProcedures` (الرأسية + بيانات المريض + حالة المريض + جدول العناصر **باستثناء مواد فئة "التحاليل المخبرية"** + الإجمالي الفرعي/الخصم/الإجمالي النهائي — تظهر دائماً)، `printPagePrescription` (روشتة)، `printPageLabTests` (مواد فئة "التحاليل المخبرية" فقط من عناصر الفاتورة، بجدول مستقل وإجمالي فرعي خاص بها)، `printPageReport` (التقرير الطبي) — الثلاث الأخيرة تظهر فقط إن كان محتواها غير فارغ (يُتخطى تماماً طبعها كصفحة، بدون صفحة بيضاء). تصنيف كل مادة بالفاتورة كـ"إجراء" أو "تحليل مخبري" وقت الطباعة يتم بفحص عضويتها الحالية بقائمة `getCatalog()[LAB_TESTS_CATEGORY]` (وليس بحقل مخزّن على الفاتورة نفسها — أي لو حُذفت المادة من فئة التحاليل بالكتالوج لاحقاً، ستُطبع كـ"إجراء" عادي مستقبلاً). `page-break-after` يُحسب ديناميكياً بجافاسكربت بعد تحديد أي الصفحات ظاهرة: كل صفحة ظاهرة تاخذ `always` إلا آخر صفحة ظاهرة فتاخذ `auto`، لتجنّب صفحة فارغة زائدة بنهاية الطباعة. لا تغيير على نافذة "عرض فاتورة سابقة" على الشاشة (`viewInvoiceModal` / `pi-table-onscreen`) — لا تزال تعرض كل العناصر بجدول واحد، لأن التعدد بالصفحات مقصور على الطباعة الورقية فقط.

- **2026-09-16:** أربع ميزات متتالية أكملت باقي البنود المتفق عليها (ما عدا الطباعة المنفصلة لكل قسم)، بناءً على طلب المستخدم:
  1. **الكشفية الثابتة + زر "مراجعة":** فئة كتالوج جديدة `رسوم ثابتة` (المتغير البرمجي `FIXED_FEE_CATEGORY`) تحوي مادة وحيدة `كشفية` (`EXAM_FEE_NAME`) بسعر افتراضي 10 د.أ مزروع لمرة واحدة (`EXAM_FEE_DEFAULT_PRICE`)، تُدار من شاشة "تعديل الأسعار" كأي مادة لكنها مستبعدة من تبويب "الإجراءات" (بنفس أسلوب استبعاد "التحاليل المخبرية"). تُضاف تلقائياً لأي فاتورة عادية جديدة (`selectedItems.set(EXAM_FEE_NAME, 1)`) بدون أي واجهة تسمح بإلغائها. زر "مراجعة" جديد ببطاقة المريض (`revisitBtn`) يظهر فقط إذا كان عمر آخر فاتورة للمريض ≤ 5 أيام (`REVISIT_WINDOW_DAYS`)؛ الضغط عليه يفتح شاشة إدخال الفاتورة العادية تماماً لكن بمتغير `isRevisitInvoice = true` فلا تُضاف الكشفية إطلاقاً. شارة ملوّنة (`fixedFeeBadge`) أعلى شاشة العناصر تُظهر الحالة الحالية (مفعّلة/معطّلة). الفاتورة المحفوظة فيها حقل `is_revisit` (بدون تأثير رجعي إن غيّر المدير سعر الكشفية لاحقاً — تُحسب دائماً بالسعر الحالي وقت الحفظ، كبقية المواد).
  2. **شاشة "الزبائن":** تبويب جديد بالقائمة الجانبية (مضاف كشاشة قابلة للتحكم بالصلاحيات ضمن `CONFIGURABLE_SCREENS`، يديرها المدير من "إدارة الصلاحيات" الموجودة أصلاً) يسرد كل المرضى المسجّلين أبجدياً مع مربع بحث بالاسم أو الهاتف، وخانة اسم قابلة للتعديل المباشر (تُحفظ فوراً بمجرد الخروج من الخانة) لكل زبون. الضغط على أي صف (غير خانة الاسم نفسها) يفتح بطاقة ذاك الزبون وفواتيره السابقة (نفس `openPatientStep`).
  3. **فاتورة سريعة IM:** رابط "إنشاء فاتورة سريعة IM" بشاشة إدخال رقم الهاتف يفتح نافذة منبثقة (`quickInvoiceModal`) مستقلة تماماً عن تدفّق الفاتورة العادي — تحوي فقط اسم مريض (نص حر، بدون رقم هاتف ولا ربط بسجل مريض)، حتى 4 خانات اختيار لمواد استهلاكية (بدون سعر فردي لكل مادة، `price: 0`)، وزرّي اختيار قيمة إجمالية ثابتة (٢ أو ٤ دينار). المواد الأربع التي تظهر بالنافذة تُحدَّد مسبقاً من المدير عبر بطاقة إعدادات جديدة بشاشة "تعديل الأسعار" (`quickInvoiceSettingsForm` — أربع قوائم منسدلة، مقصورة على مواد فئات "الإجراءات" العادية فقط، مستبعد منها التحاليل المخبرية والكشفية)، وتُحفظ بمستند Firestore جديد `ep_store/quick_invoice_settings`. الفاتورة المحفوظة `patient_id: null`، وفيها علامة `is_quick_im: true` — تظهر بشاشتي "الفواتير" و"غير المدفوعة" بشكل طبيعي (بالاعتماد على `patient_name` المخزّن مباشرة بالفاتورة) لكن لن تظهر أبداً ببطاقة أي مريض مسجّل ولا بشاشة "الزبائن" (لأنها غير مرتبطة بسجل مريض أصلاً).
  4. **تعديل الفاتورة خلال دقيقتين:** زر "تعديل الفاتورة" بنافذة معاينة أي فاتورة (`editInvoiceBtn`) يظهر فقط إذا كان عمر الفاتورة ≤ دقيقتين من وقت إنشائها (`INVOICE_EDIT_WINDOW_MINUTES`) — يُعاد فحص المهلة مرة ثانية وقت الحفظ الفعلي احتياطاً (تحسّباً لتأخّر المستخدم بالتعديل). للفواتير العادية يُعاد فتح شاشة عناصر الفاتورة الكاملة معبّأة بكل البيانات الأصلية (تشمل التبويبات الأربعة الجديدة) عبر دالة جديدة `openItemsStepForEdit(inv)`، وللفواتير السريعة IM تُعاد نافذتها الخاصة معبّأة عبر `openQuickInvoiceForEdit(inv)`. الحفظ في وضع التعديل **يحدّث نفس سجل الفاتورة** (نفس `id`/`date`/`created_by`/حالة الدفع) بدل إنشاء فاتورة جديدة، ويضيف `last_edited_at`/`last_edited_by`. **قيد معروف:** التعديل يصفّر أي خصم كان مطبّقاً على الفاتورة الأصلية (يحتاج المستخدم إعادة تطبيقه يدوياً إذا أراد إبقاءه) — تبسيط مقصود لتفادي تعقيد استرجاع نسبة الخصم بالضبط من `discount_amount` المخزّن.
  - **متبقٍ أخيراً بند وحيد من كل البنود المتفق عليها سابقاً (اكتمل الآن — انظر تحديث 2026-09-16 التالي أعلاه):** الطباعة المنفصلة لكل قسم (عناصر/روشتة/تحاليل مخبرية/تقرير طبي) بصفحة مستقلة عند الطباعة، بدل ظهورها كلها بنفس الصفحة حالياً.

- **2026-09-15:** إعادة هيكلة تبويبات شاشة "عناصر الفاتورة" (كانت البند الأول بترتيب المهمات المتفق عليه، مع دمج بندين آخرين معها)، بناءً على طلب المستخدم:
  - **قبل التعديل:** كل فئة من فئات الكتالوج (مستلزمات عامة، حقن وقساطر، أدوية...) كانت تظهر كتبويب مستقل بالقائمة الجانبية (بالإضافة لتبويب "معلومات المريض" الثابت).
  - **بعد التعديل:** أصبحت القائمة الجانبية 5 تبويبات ثابتة: **معلومات المريض** (بدون تغيير) ثم **الإجراءات** (كل فئات الكتالوج القديمة مجموعة بتبويب واحد، مع صف أزرار فلترة "الكل" + اسم كل فئة أعلى القائمة، والفلترة تُظهر/تُخفي فقط دون إعادة بناء DOM) ثم **روشتة** (حقل نص حر كبير `invoicePrescription`، 16 سطر، للطبيب) ثم **التحاليل المخبرية** (فئة كتالوج خاصة جديدة `التحاليل المخبرية` — 150 تحليل مخبري بأسعارها زُرعت لمرة واحدة تلقائياً بـ `runOneTimeSeeding()` من قائمة أسعار زوّدنا إياها المستخدم؛ السعر المزروع لكل تحليل = (سعر المختبر + 1) × 2، وقابل للتعديل لاحقاً من شاشة "تعديل الأسعار" العادية كأي مادة أخرى؛ التبويب فيه مربع بحث نصي لأن القائمة طويلة) ثم **التقرير الطبي** (حقل نص حر كبير `invoiceReport`، 16 سطر، للطبيب).
  - **الحفظ:** الفاتورة المحفوظة صار فيها حقلان جديدان `prescription` و`medical_report` (نص حر، قد يكونا فارغين). يظهران بنافذة معاينة فاتورة سابقة والنسخة المطبوعة (فقط إن لم يكونا فارغين)، بنفس أسلوب حقل "حالة المريض" الموجود مسبقاً.
  - **ملاحظة معمارية:** فئة "التحاليل المخبرية" لا تزال جزءاً من نفس كتالوج Firestore (`ep_store/item_catalog`) وتُدار بنفس شاشة "تعديل الأسعار" (إضافة/حذف/تعديل سعر) — الاستثناء الوحيد أنها مستبعدة من تبويب "الإجراءات" وتُعرض بتبويبها المستقل بدلاً من ذلك، بفلترة كود بسيطة (`LAB_TESTS_CATEGORY`) وليس ببنية بيانات منفصلة.
  - **ملاحظة:** قائمة "المتبقي" أدناه صارت قديمة — انظر تحديث 2026-09-16 أعلاه لآخر حالة (لم يبقَ سوى الطباعة المنفصلة لكل قسم). (متبقٍ حسب وضع هذا التاريخ 2026-09-15: نافذة تعديل الفاتورة خلال دقيقتين من إنشائها، الطباعة المنفصلة لكل قسم بصفحة مستقلة، وخيار "فاتورة سريعة".)

- **2026-09-12 (تحديث سابع):** ميزتان بشاشة "تعديل الأسعار"، بناءً على طلب المستخدم:
  1. **مادة واحدة بأكثر من فئة:** سحب مادة وإفلاتها على فئة مختلفة عن فئتها الأصلية صار **يضيفها لتلك الفئة أيضاً** (بدل رسالة المنع "لا يمكن نقل مادة بين فئتين مختلفتين بالسحب حالياً") — تبقى المادة بفئتها الأصلية كمان، وتشترك بنفس السعر أينما ظهرت (لأن التسعير مربوط باسم المادة لا بالفئة). عُدِّلت `deleteCatalogItem()` بالتبعية بحيث لا تحذف السعر المشترك من `ep_store/item_prices` إلا لو لم تعد المادة موجودة بأي فئة إطلاقاً بعد الحذف. عُدِّل أيضاً `updateItemRowDisplay()` بشاشة الفاتورة الجديدة ليحدّث **كل** نسخ المادة المعروضة (بأي فئة ظهرت فيها) معاً بدل أول نسخة فقط.
  2. **تحديد فئة كاملة بالفاتورة:** صندوق اختيار جديد بجانب كل فئة بشاشة "تعديل الأسعار" (معطّل افتراضياً)، يُحفظ التفعيل بمستند Firestore جديد `ep_store/category_select_all`. الفئة المفعّلة هيك تظهر بجانب اسمها بشاشة الفاتورة الجديدة صندوق "تحديد الكل" يختار/يلغي كل مواد الفئة دفعة واحدة، ويتحدّث تلقائياً (تشيك/فك تشيك) حسب حالة تحديد مواد الفئة فردياً.

- **2026-09-12 (تحديث سادس):** دعم الكميات عند إدخال الفواتير، بناءً على طلب المستخدم — كل مادة بشاشة اختيار عناصر الفاتورة صار عندها زري `+`/`−` بجانب صندوق التحديد بدل تحديد ثنائي (محدد/غير محدد) فقط. أول تحديد يبدأ بكمية 1، والسعر الظاهر جنب كل مادة هو سعر السطر الكامل (وحدة × كمية) ويتحدّث فوراً. `getSubtotal()`, حساب الخصم, الإجمالي, وحفظ الفاتورة كلهم أصبحوا يحسبون على أساس الكمية. الفاتورة المحفوظة (`items[].quantity`), نافذة المعاينة, والنسخة المطبوعة أضيف لهم عمود "الكمية" — التفاصيل الكاملة بنموذج بيانات Invoice أعلاه. **ملاحظة معمارية:** تحديثات +/- على الكمية تُحدّث صف المادة المتأثرة فقط بالـ DOM (`updateItemRowDisplay`) بدل إعادة بناء كامل الكتالوج (`renderItemsCatalog`) في كل ضغطة، للحفاظ على أداء أفضل وعدم فقدان موضع التمرير في كتالوج طويل.

- **2026-09-12 (تحديث خامس):** خصم على الفاتورة، بناءً على طلب المستخدم:
  - **من شاشة "تعديل الأسعار":** بطاقة جديدة "إعدادات الخصم" — المدير يختار نوع الخصم (قيمة ثابتة بالدينار **أو** نسبة مئوية، واحد منهم بس) ويحدد أقصى قيمة/نسبة مسموح بها. تُحفظ بمستند `ep_store/discount_settings` جديد ({type, maxValue}). القيمة الافتراضية 0 (الخصم معطّل حتى يفعّله المدير).
  - **من شاشة الفاتورة الجديدة:** خط أعداد (Range Slider) بأربع نقاط ثابتة فقط: 0% / 25% / 50% / 100% من أقصى خصم مسموح — وليس تدرّجاً حراً، بناءً على طلب المستخدم صراحةً. يظهر تلقائياً فوق زر "حفظ وطباعة الفاتورة" فقط لو كان في خصم مفعّل (`maxValue > 0`)، ويختفي كلياً غير هيك. سحب الخط يحدّث الإجمالي المعروض مباشرة (`subtotal − discountAmount`)، ويتصفّر تلقائياً (يرجع لنقطة 0%) مع كل فاتورة جديدة.
  - **الحفظ:** الفاتورة المحفوظة تخزّن `subtotal`, `discount_type`, `discount_percent_applied` (فقط للنسبة المئوية), `discount_amount`, و`total` النهائي بعد الخصم — التفاصيل الكاملة بقسم "نموذج البيانات" أعلاه.
  - **العرض:** نافذة معاينة فاتورة سابقة والنسخة المطبوعة كلاهما يعرضان الآن سطر "الإجمالي الفرعي" وسطر "الخصم" منفصلين عن "الإجمالي" النهائي، لكن فقط إذا كان في خصم مطبَّق فعلياً على الفاتورة (الفواتير القديمة قبل هذه الميزة تُعامَل بأمان كأنها بدون خصم).
  - **التحديث الحي:** أي تعديل على إعدادات الخصم من جهاز المدير ينعكس فوراً (`onSnapshot`) على أي جهاز آخر مفتوح حالياً على شاشة "تعديل الأسعار" (إعادة عرض القيم) أو شاشة "فاتورة جديدة" (إعادة حساب الإجمالي المعروض فوراً بنفس نقطة الخط الحالية).

- **2026-09-12 (تحديث رابع):** الانتقال الفعلي إلى Firebase Firestore بدل localStorage، بناءً على طلب المستخدم — التفاصيل الكاملة موثّقة بقسم "القرارات التقنية". بالإضافة لذلك: تحويل بطاقة "مرضى مسجّلون" بالشاشة الرئيسية إلى بطاقة **"مرضى بالانتظار"** (تعرض عدد قائمة الانتظار الحالية `ep_store/waiting_list` بدل عدد كل المرضى المسجّلين تاريخياً)، وربطها بالضغط لفتح شاشة "مرضى بالانتظار" مباشرة (بنفس طريقة بطاقة "فواتير غير مدفوعة")، وتحديثها بشكل حي إن تغيّرت قائمة الانتظار من جهاز آخر أثناء عرض الشاشة الرئيسية.
- **2026-09-12 (تحديث ثالث):** نشر الموقع أونلاين لأول مرة، بناءً على طلب المستخدم:
  - رُفع الكود على مستودع GitHub جديد باسم `emergencyplus`.
  - أُنشئ موقع استضافة على Netlify باسم `eplus-life`، ورُبط بالمستودع ربطاً تلقائياً (Continuous Deployment) — أي تحديث يُرفع على GitHub يُنشر تلقائياً على الموقع المباشر خلال دقيقة أو دقيقتين، بدون رفع يدوي.
  - الموقع شغّال حالياً على الرابط المؤقت `https://eplus-life.netlify.app`.
  - **متبقٍ:** ربط الدومين المخصص `www.eplus.life` (مسجّل على GoDaddy) بالموقع — لم يُنفَّذ بعد. التفاصيل والخطوات المتبقية موثّقة بقسم "النشر والاستضافة" ضمن القرارات التقنية أعلاه.
  - **ملاحظة:** Claude لا يملك أداة متصلة للوصول المباشر لحساب GitHub — أي تعديل مستقبلي على الكود يتطلب من المستخدم رفعه يدوياً (خطوة بسيطة: فتح الملف على GitHub، تعديل، Commit).

- **2026-09-10:** تم تصميم وبناء شاشة تسجيل الدخول والشاشة الرئيسية (Dashboard) كنموذج أولي في `index.html`.
- **2026-09-10:** تم بناء تدفق "فاتورة جديدة" بالكامل:
  1. البحث برقم الهاتف.
  2. إن لم يوجد المريض → نافذة منبثقة لتسجيله (اسم + عمر، والهاتف مأخوذ تلقائياً من خطوة البحث) ويُحفظ في `ep_patients`.
  3. عرض بطاقة المريض + قائمة فواتيره السابقة (إن وُجدت) من `ep_invoices`.
  4. زر "إدخال فاتورة" → شاشة قائمة عناصر (كتالوج الأدوية والمستلزمات من الصورة المرفقة) بنظام تحديد (checkbox)، سعر كل عنصر 10 د.أ مؤقتاً، مع إجمالي حي.
  5. زر "حفظ وطباعة" → يحفظ الفاتورة في `ep_invoices`، ويولّد رقم فاتورة تسلسلي، ثم يفتح نافذة الطباعة (`window.print`) بتنسيق مخصص للطباعة فقط.
  - باقي أزرار الشاشة الرئيسية (بحث عن مريض، تسجيل مريض جديد، كل الفواتير) لا تزال تعرض رسالة "قيد الإنشاء" لأنها لم تُبنَ كشاشات مستقلة بعد.

- **2026-09-10:** تعديل شاشة "تعديل الأسعار" بناءً على طلب المستخدم:
  - أُلغيت ميزتا "تعيين سعر موحد لكل العناصر" و"استعادة السعر الافتراضي".
  - أُضيف بدلاً منهما نموذج "إضافة مادة جديدة" (اسم + فئة + سعر اختياري).
  - أُضيف زر حذف مادة (مع تأكيد) بجانب كل عنصر في القائمة.
  - أصبح كتالوج العناصر نفسه محفوظاً ديناميكياً في `ep_item_catalog` بدل أن يكون قائمة ثابتة بالكود، ليدعم الإضافة والحذف.

- **2026-09-10:** إضافات بناءً على طلب المستخدم (اسم المريض لكل فاتورة + مرضى الانتظار):
  - **شاشة عناصر الفاتورة:** أُضيفت خانة "اسم المريض لهذه الفاتورة" (تُعبّأ تلقائياً باسم المريض الأساسي وقابلة للتعديل)، لمعالجة حالة اشتراك أكثر من مريض بنفس رقم الهاتف (كالأم وطفلها). القيمة تُحفظ في الفاتورة نفسها (`patient_name`) وتظهر في الطباعة.
  - **الفواتير السابقة (داخل بطاقة المريض):** أصبحت تعرض اسم المريض الفعلي لكل فاتورة (وليس فقط التاريخ والمبلغ والحالة).
  - **بطاقة "تسجيل مريض جديد" بالشاشة الرئيسية:** أُلغيت واستُبدلت ببطاقة "مرضى بالانتظار" (وكذلك رابط القائمة الجانبية المقابل لها).
  - **بطاقة المريض (خطوة 2 من الفاتورة):** أُضيف زر "أضف مريض بالانتظار" يضيف المريض الحالي لقائمة انتظار مشتركة.
  - **شاشة "مرضى بالانتظار" الجديدة:** تعرض كل المرضى المنتظرين (الاسم، الهاتف، وقت الإضافة). الضغط على أي مريض ينقل مباشرة لبطاقته (فواتير سابقة + إدخال فاتورة جديدة). يوجد أيضاً زر حذف يدوي لكل عنصر.

- **2026-09-10:** تعديلان إضافيان بناءً على طلب المستخدم:
  - **قاعدة حذف مرضى الانتظار:** تم تصحيح السلوك — الضغط على اسم مريض من شاشة "مرضى بالانتظار" لم يعد يحذفه تلقائياً من القائمة (كان هذا السلوك القديم). الآن المريض يبقى في القائمة حتى يُحذف يدوياً بزر (✕)، أو تُنشأ له فاتورة فعلية بنجاح (عندها يُحذف تلقائياً بدالة `removeWaitingByPatientId`، بغض النظر عن المسار الذي وصل منه لبطاقته).
  - **شاشة "تعديل الأسعار":** أُضيفت إمكانية:
    1. إضافة فئة جديدة فارغة (نموذج مستقل بجانب نموذج إضافة المادة).
    2. إعادة ترتيب الفئات بالسحب والإفلات (مقبض ⠿ في عنوان كل فئة).
    3. إعادة ترتيب المواد داخل نفس الفئة بالسحب والإفلات (مقبض ⠿ بجانب كل مادة). النقل بين فئتين مختلفتين بالسحب غير مدعوم حالياً.

- **2026-09-10:** خمس تعديلات بناءً على طلب المستخدم:
  1. **"أضف مريض بالانتظار":** أصبح الضغط عليه ينقل مباشرة لشاشة "مرضى بالانتظار" (بدل البقاء بشاشة الفاتورة).
  2. **تبويب "حسابي":** أُضيف للقائمة الجانبية ويحتوي معلومات المستخدم الحالي وزر "تسجيل خروج" — وأُزيل زر تسجيل الخروج نهائياً من الشريط العلوي في باقي الشاشات.
  3. **تعديل اسم الفئة:** شاشة "تعديل الأسعار" أصبحت تسمح بتعديل اسم أي فئة مباشرة (خانة نص بدل نص ثابت)، مع منع الأسماء الفارغة أو المكررة.
  4. **فصل شاشة بطاقة المريض لشاشتين مستقلتين ومميّزتين بصرياً:**
     - `invoiceStepPatient` (ضمن تدفق "فاتورة جديدة"): شارة بلون أساسي "فاتورة جديدة"، وزر "تغيير المريض" يرجع لشاشة إدخال رقم الهاتف.
     - `view-waiting-patient` (شاشة مستقلة جديدة، تُفتح من "مرضى بالانتظار"): شارة ولون شريط جانبي كهرماني مميّز "من قائمة الانتظار"، وزر "رجوع لقائمة الانتظار" يرجع لشاشة الانتظار مباشرة.
     - الشاشتان تشتركان فقط بشاشة "عناصر الفاتورة"، ويتحكم بالتنقل بينهما متغيّر `invoiceEntrySource`. بعد حفظ الفاتورة، يعود المستخدم تلقائياً للشاشة المناسبة (قائمة الانتظار أو الشاشة الرئيسية) حسب مصدر الدخول.
  5. **عرض الفاتورة السابقة للقراءة فقط:** الضغط على أي فاتورة بقائمة "الفواتير السابقة" (بالشاشتين) يفتحها بنافذة منبثقة للعرض فقط (بدون أي تعديل)، مع زر طباعة مباشر.

- **2026-09-10:** خمس تعديلات أخرى بناءً على طلب المستخدم:
  1. **التحقق من رقم الهاتف:** شاشة البحث برقم الهاتف ترفض الآن أي رقم لا يطابق نمط "10 خانات تبدأ بـ 07" (`PHONE_REGEX = /^07\d{8}$/`)، مع رسالة خطأ واضحة، بدل قبول أي رقم من خانة واحدة كما كان سابقاً.
  2. **رسالة تأكيد عند "أضف مريض بالانتظار":** أصبح الزر يعرض نافذة تأكيد (استُبدلت لاحقاً بنافذة مصممة خاصة بالتطبيق — انظر سجل اليوم التالي) تذكر اسم المريض صراحة قبل إضافته لقائمة الانتظار، لتفادي إضافة مريض بالخطأ.
  3. **تفعيل بطاقة "فواتير غير مدفوعة":** أصبحت بطاقة الإحصائية بالشاشة الرئيسية قابلة للضغط، وتفتح شاشة جديدة (`view-unpaid`) تسرد كل الفواتير غير المدفوعة مع زر "تحديد كمدفوعة" لكل فاتورة (يحدّث الفاتورة في `ep_invoices` فورياً ويُحدّث الإحصائيات)، بالإضافة لإمكانية الضغط على الفاتورة نفسها لعرضها بنافذة القراءة فقط.
  4. **بطاقة "حالة المريض":** أُضيفت لشاشة "عناصر الفاتورة" (حقل نصي حر يكتب فيه الطبيب وصف الحالة/التشخيص)، تُحفظ في حقل `condition` بالفاتورة، وتظهر عند الطباعة وعند عرض الفاتورة بنافذة القراءة فقط (فقط إن لم تكن فارغة).
  5. **تحويل كل نصوص الواجهة من صيغة المخاطب المؤنث إلى المذكر** (مثال: "ابحثي" → "ابحث"، "أدخلي" → "أدخل"، "موظفات الاستقبال" → "موظفي الاستقبال")، لأن التطبيق يُستخدم من أكثر من موظف وليس بالضرورة موظفة واحدة.

- **2026-09-10:** تحسين على نافذة تأكيد "أضف مريض بالانتظار" بناءً على توضيح المستخدم للغرض الحقيقي من الميزة:
  - **السبب:** بما أن رقم هاتف واحد قد يخص أكثر من مريض (الأم وطفلها)، فالغرض من "رسالة التأكيد" ليس مجرد تأكيد نعم/لا، بل تمكين الموظف من **تحديد/تعديل اسم المريض الفعلي** الذي سيصل للطبيب (مثلاً كتابة اسم الطفل بدل الاسم المسجّل على الرقم وهو اسم الأم).
  - استُبدلت نافذة `confirm()` الافتراضية من المتصفح بنافذة مصممة (`confirmWaitingModal`) تحتوي حقل نص معبّأ مسبقاً باسم المريض الأساسي، لكنه قابل للتعديل الكامل قبل الضغط على "تأكيد الإضافة".
  - الاسم المُدخل في هذه النافذة يُحفظ كـ `patient_name` بمدخل قائمة الانتظار، ويظهر بنفس الاسم في: صف قائمة "مرضى بالانتظار"، وعنوان بطاقة المريض عند فتحها من هناك، والتعبئة المبدئية لحقل "اسم المريض لهذه الفاتورة" عند إنشاء الفاتورة له. بهذا الشكل الطبيب يرى الاسم الصحيح للمريض في كل خطوة، وليس فقط اسم صاحب/صاحبة رقم الهاتف.

- **2026-09-12 (تحديث ثانٍ):** صلاحيات ديناميكية قابلة للتخصيص + تغيير كلمة المرور + تغيير اسم الحساب الافتراضي، بناءً على طلب المستخدم:
  - **نظام صلاحيات (`ep_roles`) بدل نص ثابت "admin/staff":** المدير (صلاحية "مدير" المدمجة الثابتة) يقدر يضيف صلاحيات جديدة بأي اسم يريده (مثال: محاسب، طبيب) من شاشة جديدة "إدارة الصلاحيات" بالقائمة الجانبية (للمدير فقط). لكل صلاحية جديدة يحدد المدير أي شاشات من الخمس التشغيلية (فاتورة جديدة، الفواتير، مرضى بالانتظار، تقييم المرضى، تعديل الأسعار) مسموح لأصحابها برؤيتها، عبر مربعات اختيار. يمكن تعديل صلاحيات أي دور موجود (باستثناء "مدير" نفسه، ثابت دائماً بكل الشاشات) وحفظ التعديلات، أو حذف الدور بالكامل إن لم يكن مرتبطاً بأي حساب حالياً.
  - **القائمة الجانبية تتحدّث فوراً** حسب صلاحية المستخدم المسجّل دخوله — أي شاشة غير مسموحة لصلاحيته تختفي تلقائياً من القائمة، وأي محاولة وصول مباشر لها محمية بالكود أيضاً (`hasAccess`).
  - شاشة "إدارة المستخدمين": حقل "الصلاحية" عند إضافة حساب جديد أصبح قائمة منسدلة ديناميكية تعرض كل الصلاحيات الموجودة فعلياً (المدمجة والمخصصة)، بدل خيارين ثابتين فقط.
  - **تغيير كلمة المرور من شاشة "حسابي":** نموذج جديد يطلب كلمة المرور الحالية (للتحقق) + كلمة المرور الجديدة + تأكيدها. متاح لأي مستخدم على حسابه الشخصي فقط (لا يوجد إعادة تعيين من طرف المدير حالياً — انظر البند 9 بالخطوات القادمة).
  - **الحساب الافتراضي المزروع أصبح `Admin` بدل `reception`:** غيّرنا اسم المستخدم الافتراضي المزروع تلقائياً عند أول تشغيل من `reception` إلى `Admin` (كلمة المرور وبقية البيانات لم تتغير: `12345`، الاسم الكامل "سارة"، صلاحية مدير). أضفنا أيضاً ترحيلاً تلقائياً لمرة واحدة يحوّل أي حساب موجود مسبقاً باسم مستخدم `reception` إلى `Admin`، حتى لا تفقد الأجهزة التي كانت تستخدم النظام قبل هذا التحديث القدرة على الدخول.

- **2026-09-12:** بناء نظام حسابات مستخدمين متعدد حقيقي (كان أول بند بقائمة "الخطوات القادمة"):
  - حساب `DEMO_USER` الثابت بالكود أُلغي بالكامل، واستُبدل بمصفوفة حسابات `ep_users` في localStorage (`{id, username, password, fullName, role}`). أول تشغيل يزرع الحساب الافتراضي القديم (`reception`/`12345`، الاسم "سارة") تلقائياً كأول حساب بصلاحية `admin`، حتى لا تنكسر بيانات الفواتير القديمة المرتبطة باسمه.
  - تسجيل الدخول أصبح يبحث فعلياً بمصفوفة `ep_users` بدل مقارنة ثابتة. الجلسة تُحفظ بـ `sessionStorage` تحت `ep_current_user` (الحساب كاملاً) بدل علم بسيط، ويُعاد التحقق من وجود الحساب فعلياً عند إعادة تحميل الصفحة (تحسّباً لحذفه بواسطة المدير أثناء الجلسة).
  - كل مكان كان يستخدم `DEMO_USER.fullName` (الترحيب، بطاقة "حسابي"، حقل `created_by` بالفاتورة، فلترة شاشة "الفواتير" اليومية) أصبح يقرأ من `currentUser` الفعلي.
  - **شاشة "إدارة المستخدمين" الجديدة:** رابط مستقل بالقائمة الجانبية (بناءً على اختيار المستخدم)، **يظهر فقط لحساب بصلاحية `admin`** (مخفي بـ CSS لغير المدير، ومحمي أيضاً بالكود لو حاول أحد الوصول له مباشرة). تحتوي نموذج إضافة حساب جديد (اسم كامل، اسم مستخدم، كلمة مرور، صلاحية مدير/موظف استقبال) وقائمة الحسابات الحالية مع زر حذف لكل حساب.
  - **قواعد الحذف (بناءً على اختيار المستخدم):** لا يمكن للمدير حذف حسابه الحالي، ولا حذف آخر حساب مدير متبقٍ بالنظام (لمنع فقدان الوصول لشاشة الإدارة بالكامل). حذف حساب موظف عادي **لا يمسح فواتيره القديمة** — تبقى محفوظة بنفس اسمه في `created_by` كما هي (بناءً على اختيار المستخدم أيضاً).
  - بطاقة "حسابي" أصبحت تعرض صلاحية الحساب الفعلية (مدير/موظف استقبال) بدل نص ثابت "موظف استقبال".

- **2026-09-10:** ثلاث ميزات جديدة + إعادة توزيع بطاقتين بالشاشة الرئيسية بناءً على طلب المستخدم:
  1. **طريقة الدفع عند "تحديد كمدفوعة":** بدل تحديد الفاتورة كمدفوعة مباشرة، يفتح الزر الآن نافذة مصممة (`paymentMethodModal`) تعرض ثلاثة خيارات (كاش / فيزا / كليك). اختيار أي منها يحدّد الفاتورة كمدفوعة فوراً ويحفظ الطريقة في حقل `payment_method` بالفاتورة.
  2. **بطاقة "تقييم المرضى" (محل "كل الفواتير" سابقاً):** شاشة جديدة (`view-ratings`) تسرد كل الفواتير (اسم المريض، الهاتف، مبلغ الفاتورة، التاريخ) مع نجوم تقييم (1-5) قابلة للضغط أمام كل فاتورة. الغرض: يتصل الموظف بالمريض بعد الزيارة ويسجّل تقييمه مباشرة. التقييم يُحفظ في حقل `rating` بالفاتورة نفسها، وقابل للتعديل لاحقاً بإعادة الضغط على تقييم مختلف.
  3. **بطاقة "الفواتير" (محل "بحث عن مريض" سابقاً):** شاشة جديدة (`view-my-invoices`) تعرض فقط الفواتير التي أنشأها **المستخدم الحالي** (`created_by`) **خلال اليوم الحالي**. كل فاتورة قابلة للضغط لفتحها بنافذة القراءة فقط. هذه الميزة مصمَّمة لدعم أكثر من موظف يستخدمون النظام، كل واحد يشوف فواتيره اليومية فقط.
  - **ملاحظة معمارية:** بما أن تسجيل الدخول حالياً حساب تجريبي واحد مشترك (`reception`/`12345`)، فكل الفواتير حالياً `created_by = 'سارة'`، فالفلترة تعمل تقنياً لكنها لا تُظهر فرقاً فعلياً بين موظفين حقيقيين إلى أن يُبنى نظام حسابات متعدد المستخدمين (انظر البند التالي في الخطوات القادمة).

## 7. الخطوات القادمة المقترحة
1. **إكمال ربط الدومين المخصص `www.eplus.life`** بموقع Netlify (`eplus-life`) — الخطوات موثّقة بقسم "النشر والاستضافة". هذه أولوية حالية مفتوحة.
2. ~~ربط تخزين مشترك حقيقي (Firebase) بدل localStorage~~ — ✅ **تم تنفيذه بتاريخ 2026-09-12.** التفاصيل بقسم "القرارات التقنية". الأولوية الأمنية المتبقية منه: إضافة Firebase Authentication وتضييق Firestore Rules (حالياً مفتوحة بالكامل).
3. بناء شاشة "عرض كل الفواتير" شاملة وقابلة للفلترة/البحث بكل الحالات معاً (مدفوعة/غير مدفوعة/كل التواريخ) إن احتاجت العيادة ذلك لاحقاً، منفصلة عن شاشتي "الفواتير" (اليومية) و"تقييم المرضى" الحاليتين.
4. التفكير في ما إذا كانت العيادة تحتاج شاشة بحث عن مريض وشاشة تسجيل مريض جديد كشاشتين مستقلتين أيضاً (منفصلتين عن تدفق الفاتورة وقائمة الانتظار) أو أن الوضع الحالي كافٍ.
5. مراجعة قائمة العناصر مع العيادة للتأكد من دقتها الكاملة (تم استخراجها من صورة نموذج ورقي وقد تحتاج تصحيحات بسيطة في الإملاء/الأسماء).
6. إضافة إمكانية حذف فئة كاملة من شاشة "تعديل الأسعار" إن احتاجت العيادة ذلك لاحقاً (غير موجودة حالياً — فقط الإضافة، إعادة التسمية، والترتيب).
7. إن احتاجت العيادة استخدام شاشة "تعديل الأسعار" من جهاز لمسي (تابلت/جوال)، سيلزم استبدال السحب والإفلات (HTML5 Drag & Drop) بأزرار "تحريك لأعلى/لأسفل" لأنه غير مدعوم جيداً باللمس.
8. صيغة نمط رقم الهاتف (`07` + 8 أرقام) مبنية على نمط الأرقام الأردنية الشائع — إن كانت العيادة تستقبل أرقاماً بصيغ أخرى (دولية، شبكات مختلفة)، يلزم تعديل `PHONE_REGEX` لاحقاً.
9. لا توجد بعد تقارير أو ملخصات لتقييمات المرضى (مثل متوسط التقييم لكل موظف/شهر) — حالياً كل ما هو متاح هو تسجيل التقييم على مستوى الفاتورة الواحدة فقط.
10. لا توجد حالياً إمكانية "تعديل" حساب موجود من شاشة "إدارة المستخدمين" (تغيير الاسم الكامل، اسم المستخدم، أو صلاحيته بعد الإنشاء) — فقط الإضافة والحذف. تغيير كلمة المرور فقط أصبح متاحاً، لكن من الموظف نفسه عبر شاشة "حسابي" (يتطلب معرفة كلمة المرور الحالية)، وليس من المدير مباشرة. لو موظف نسي كلمة مروره بالكامل، لا توجد حالياً طريقة لإعادة تعيينها إلا بحذف حسابه وإنشاء حساب جديد.
11. لا يوجد حالياً تحقق من قوة كلمة المرور أو حد أدنى لطولها عند إضافة حساب جديد أو تغيير كلمة مرور.
12. لا يمكن حالياً إعادة تسمية اسم الصلاحية بعد إنشائها من شاشة "إدارة الصلاحيات" — فقط تعديل الشاشات المسموحة، أو الحذف الكامل (إذا لم يكن هناك حسابات مرتبطة بها) وإنشاؤها من جديد.
13. شاشتا "إدارة المستخدمين" و"إدارة الصلاحيات" مقصورتان دائماً على صلاحية "مدير" المدمجة فقط ولا يمكن منحهما لصلاحية مخصصة حالياً (قرار تصميمي متعمّد لتفادي منح صلاحية غير المدير القدرة على إنشاء حسابات/صلاحيات أخرى) — إن احتاجت العيادة لاحقاً منح موظف معيّن صلاحية "إدارة المستخدمين" فقط دون "إدارة الصلاحيات"، سيلزم تعديل هذا القرار.
14. الخصم على الفاتورة قرار نهائي وقت الحفظ فقط — لا توجد حالياً طريقة لتعديل الخصم (أو أي جزء آخر) على فاتورة محفوظة مسبقاً من واجهة النظام؛ لو احتاجت العيادة تعديل خصم فاتورة بعد حفظها، الحل الوحيد حالياً تعديل السجل مباشرة بقاعدة البيانات (Firestore Console)، وهذا غير مناسب للاستخدام اليومي.
15. لا يوجد حد أقصى مضروب فعلياً على قيمة الخصم الثابت مقارنة بمجموع الفاتورة — لو المدير حط أقصى قيمة خصم أكبر من إجمالي فاتورة صغيرة (مثال: خصم أقصى 10 د.أ على فاتورة قيمتها 5 د.أ فقط)، النظام يمنع النتيجة من أن تصير سالبة (`Math.max(0, ...)`) لكن ما بينبّه المستخدم أو المدير لوجود هذا التعارض بالإعدادات.
